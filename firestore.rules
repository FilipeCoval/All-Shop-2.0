rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // allow read and write for all collections to guarantee seamless dashboard and client experience
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
